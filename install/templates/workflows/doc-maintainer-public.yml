# Caller template for aidoc-flow-ci's AI-driven doc-maintainer workflow.
# Per IPLAN-0025 (operations 2026-06-28; READY via PR #166). Ships as
# ci/v1.4.0 alpha.1.
#
# This is the PUBLIC-runner variant (ubuntu-latest). For full rationale +
# prerequisites, see doc-maintainer-private.yml header.
#
# PUBLIC-runner specifics:
#   - Runs on ubuntu-latest (GitHub-hosted) per docs/security.md §3
#     (self-hosted runners on PUBLIC repos = accepted-risk only).
#   - The workflow calls the same LiteLLM proxy as private consumers; no
#     vendor CLI is installed on the GitHub-hosted runner.
#   - Fork PRs do NOT trigger this workflow (push: main fires only after
#     merge to default, by which point the trust gate has already routed
#     fork-authored PRs to HUMAN-REVIEW-ONLY in ai-review.yml).

name: doc-maintainer
run-name: doc-maintainer ${{ inputs.head_sha || github.sha }}

on:
  push:
    branches: [main]
  schedule:
    - cron: '7,37 * * * *'
  workflow_dispatch:
    inputs:
      head_sha:
        description: 'Merge SHA to recover (normally set by the scheduled reconciler)'
        required: false
        type: string

permissions:
  actions: write
  contents: write
  issues: write
  pull-requests: write

jobs:
  call:
    uses: vladm3105/aidoc-flow-ci/.github/workflows/doc-maintainer.yml@ci/v2.0.1
    with:
      runner_labels: '"ubuntu-latest"'
      model: ai-doc-maintainer
      merge_sha: ${{ inputs.head_sha || '' }}
    secrets: inherit  # pragma: allowlist secret
